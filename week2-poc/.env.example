# EPAM Dial (OpenAI-compatible, Azure-style routes) — all four are required.
# Copy to .env and fill in; .env is gitignored and never committed (Principle VII).
# NOTE: the Dial API works under EPAM VPN only. Deployment names are listed by
#   curl -s "https://ai-proxy.lab.epam.com/openai/models" -H "Api-Key: $API_KEY"
DIAL_BASE_URL=https://ai-proxy.lab.epam.com
DIAL_API_KEY=your-api-key
DIAL_CHAT_MODEL=gpt-4o
DIAL_EMBEDDING_MODEL=text-embedding-3-small-1

# Judge model for `npm run eval` (LLM-as-judge + baseline verdicts). Defaults to
# DIAL_CHAT_MODEL. Pin it (e.g. gpt-4o) when comparing chat models so SC-003 is
# scored by the same referee across runs instead of each model judging itself.
#DIAL_JUDGE_MODEL=gpt-4o

# Optional overrides (defaults shown).
# RAG_K — number of chunks retrieved per question.
#RAG_K=5
# REFUSAL_MIN_SCORE — cosine-similarity floor below which the assistant refuses
# (FR-004). Calibrated on the golden set (T037): answer-expected questions
# score >= 0.62 top-1, refusal-group 0.20-0.54, advice-framed ~0.45-0.48.
# 0.4 refuses clearly-off-topic questions deterministically while letting
# near-topic refusals and advice-framed questions reach synthesis, where model
# judgment refuses/handles them (FR-005). Raising it above ~0.45 would
# short-circuit the advice path.
#REFUSAL_MIN_SCORE=0.4
# RAG_ENHANCE — LLM query rewriting before retrieval (US7, off by default).
# Measure its effect with `npm run eval -- --compare` before turning it on.
#RAG_ENHANCE=false
